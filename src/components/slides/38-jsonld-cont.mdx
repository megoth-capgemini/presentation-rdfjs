import Code from "../code";
import contextExample from "./code/jsonld-context.json?raw";

### JSON-LD example, continued

<Code language="json" code={contextExample}/>

<aside className="notes">
    Here is the @context for the previous example. Contexts are usually defined in a separate document, to encourage
    reuse, but can be defined locally in the JSON-LD document as well.
</aside>