import Code from "../code";
import turtleExample from "./code/turtle-example.ttl?raw";

### Turtle example

<Code language="turtle" code={turtleExample} />

<aside className="notes">
    Turtle is designed to be human readable, and is in my mind the best way of serialize RDF.

    A line usually consist of one triple, but can have multiple (as we can see on line 7).
</aside>